import styled from "styled-components";

const MoreText = styled.p`
  margin-top: 50px;
  font-size: 18px;
  line-height: 27px;
  text-align: center;
  color: var(--primary);
  cursor: pointer;
  user-select: none;
`;

export default MoreText;
